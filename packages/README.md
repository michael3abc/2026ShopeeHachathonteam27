# Shared Packages

Reserved for non-contract runtime packages that are genuinely consumed by multiple apps. All DTOs, Provider interfaces, adapters, and generated schemas belong in [`apps/contracts`](../apps/contracts/README.md).
